---
title: "【3】数据结构与算法-3 冒泡排序算法"
date: 2016-09-06
category: 随笔杂谈
tags: []
---

### (三)、 冒泡排序算法  冒泡排序算法是一种交换排序，基本思想是：两两比较相邻记录的关键字，如果反序则交换，直到没有反序的记录为止。

```
package com.sort.exe;

public class BubbleSort {
	//1 简单版本：先设定第一个数，然后数组中剩下的数与其比较，小于这个数就交换；
	// 		   第一个数比较完毕，继续比较第二个数,i++
	public void BubbleSort(int[]  arrs){
		for(int i=0;i<arrs.length;i++){
			for(int j=i+1;j<arrs.length;j++){
				if(arrs[i] > arrs[j]){
					int temp=arrs[i];
					arrs[i]=arrs[j];
					arrs[j]=temp;
				}
					
			}
		}
	}
	
	//2 正宗的冒泡算法
	public void BubbleSort2(int[]  arrs){
		for(int i=0;i<arrs.length;i++){
			for(int j=arrs.length-2;j>=i;j--){
				if(arrs[j]>arrs[j+1]){//arrs[j+1]为倒数第一个数
					int temp=arrs[j];
					arrs[j]=arrs[j+1];
					arrs[j+1]=temp;
				}
			}
		}
	}

	//3 改良版：排好序的就不用比较</span>
	public void BubbleSort3(int[]  arrs){
		boolean flag=true;
		//第一轮冒泡排序结束后，如果flag为false,则表明没有数据交换，即除第i个数外，其余的数都是排好序的
		for(int i=0;i<arrs.length && flag;i++){
			flag=false;//初始化
			//每轮的冒泡排序结束，判断其flag
			for(int j=arrs.length-2;j>=i;j--){
				if(arrs[j]>arrs[j+1]){//arrs[j+1]为倒数第一个数
					int temp=arrs[j];
					arrs[j]=arrs[j+1];
					arrs[j+1]=temp;
					//有数据交换，则令其为true
					flag=true;
				}
			}
		}
	}
	public static void main(String[] args) {
		int[] a=new int[]{2,1,5,8,3,7,9};
		new BubbleSort().BubbleSort3(a);
		for(int i:a){
			System.out.print(i+" ");
		}
	}
}
```