---
title: "JVM(一)—Java内存模型"
date: 2017-07-22
category: 后端开发
tags: [JVM内存模型]
---

### JVM(一)—Java内存模型

Java虚拟机内存包括如下几个运行时数据区域：

![](./images/9059c167fad472bc.png)  
 （图片来源：<http://www.importnew.com/23792.html>）

- 程序计数器
- 虚拟机栈
- 本地方法栈
- 堆
- 方法区

1 程序计数器—线程私有

程序计数器是当前线程执行的字节码的行号指示器。   
 程序计数器占用一块较小的内存，线程私有。

**为什么需要 程序计数器？**  
 因为Java虚拟机的多线程是通过线程切换获得CPU执行时间来执行的。每个处理器只会处理一条线程，为了处理器切换线程时能回到之前所执行的位置，每个线程都要有一个 程序计数器，记录当前线程执行的字节码的位置。所以是线程私有的。

2 Java虚拟机栈—线程私有

虚拟机栈主要存放局部变量表，操作数栈，动态链接和方法出口等信息。其中局部变量表主要是基本数据类型和对象的引用。

1）运行执行时情况   
 每个方法在执行时，都会创建一个栈帧用于存储局部变量表，操作数栈，动态链接和方法出口等信息。每一个方法从调用到执行完成的过程，就对应着一个栈帧在Java虚拟机栈中入栈到出栈的过程。

2）两种异常   
 StackOverflowError：线程申请的栈内存过大；   
 OutofMemoryError：栈内存不够时。

3 本地方法栈—线程私有

本地方法栈中存储的是本地方法Native方法的信息。

4 Java堆—线程共享

主要用来存放对象实例和数组。   
 也是垃圾收集器管理的主要区域。

5 方法区—线程共享

用于存储被虚拟机加载的类信息、常量、静态变量、即时编译后的代码等数据。

这个区域的内存回收主要是针对常量池的回收和对类型的卸载。

运行时常量池

运行时常量池是方法区的一部分，主要存储编译期生成的各种字面量和符号引用。   
 运行期间也可能将新的常量放入池中，如String.intern()即用的这个原理。